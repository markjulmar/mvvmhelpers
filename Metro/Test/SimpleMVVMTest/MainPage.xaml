<Page
    x:Class="SimpleMVVMTest.MainPage"
    IsTabStop="false"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="using:JulMar.Windows.Converters" xmlns:Interactivity="using:System.Windows.Interactivity"
    xmlns:Interactivity1="using:JulMar.Windows.Interactivity"
    xmlns:Actions="using:JulMar.Windows.Interactivity.Actions" mc:Ignorable="d">

    <Page.Resources>
        <Converters:ToStringConverter x:Key="toString" />
    </Page.Resources>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <SolidColorBrush Color="Yellow" x:Key="rcBrush" />
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Row="1" Orientation="Horizontal">
            <Button Content="Show Text" 
                Margin="20" Padding="10,5"
                Command="{Binding ShowText}" />

            <Button Content="Adjust Rectangle" Margin="20" Padding="10,5" Click="OnAdjustRect" />

            <Button Content="Call JustShowText" Margin="20" Padding="10,5">
                <Interactivity:Interaction.Triggers>
                    <Interactivity:EventTrigger EventName="Click">
                        <Actions:CallMethodAction TargetObject="{Binding}"  MethodName="JustShowText" />
                    </Interactivity:EventTrigger>
                </Interactivity:Interaction.Triggers>
            </Button>

        </StackPanel>
        
        <CheckBox Content="Show Advanced Message" HorizontalAlignment="Right" VerticalAlignment="Center"
                  Grid.Row="1"
                  Margin="20" IsChecked="{Binding ShowAdvanced, Mode=TwoWay}" />

        <Rectangle Fill="{Binding Color}" Margin="50" Stroke="White" StrokeThickness="1" Width="200" Height="200">
            <Interactivity:Interaction.Triggers>
                <Interactivity:EventTrigger EventName="PointerEntered">
                    <Interactivity1:InvokeCommand Command="{Binding MouseEnter}" />
                </Interactivity:EventTrigger>
                <Interactivity:EventTrigger EventName="PointerExited">
                    <Interactivity1:InvokeCommand Command="{Binding MouseLeave}" />
                </Interactivity:EventTrigger>
            </Interactivity:Interaction.Triggers>
        </Rectangle>

        <Rectangle Margin="50" Stroke="White" StrokeThickness="1" Width="200" Height="200" HorizontalAlignment="Left" VerticalAlignment="Top" Fill="{StaticResource rcBrush}">
            <Interactivity:Interaction.Triggers>
                <Interactivity1:DoubleClickTrigger>
                    <Interactivity1:ChangePropertyAction TargetObject="{StaticResource rcBrush}" PropertyName="Color" Duration="0:0:3">
                        <Interactivity1:ChangePropertyAction.Value>
                            <Color>Blue</Color>
                        </Interactivity1:ChangePropertyAction.Value>
                    </Interactivity1:ChangePropertyAction>
                </Interactivity1:DoubleClickTrigger>
            </Interactivity:Interaction.Triggers>
        </Rectangle>

        <Rectangle Fill="Red" Margin="50" Stroke="White" StrokeThickness="1" Width="200" Height="200" HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <Interactivity:Interaction.Behaviors>
                <Interactivity1:DragPositionBehavior />
            </Interactivity:Interaction.Behaviors>
        </Rectangle>

        <Rectangle x:Name="GreenRect" Fill="Green" Margin="50" Stroke="White" StrokeThickness="1" Width="200" Height="200" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Interactivity:Interaction.Triggers>
                <Interactivity1:DataTrigger Binding="{Binding ShowAdvanced}" Comparison="Equal" Value="True">
                    <Interactivity1:ChangePropertyAction PropertyName="Fill">
                        <Interactivity1:ChangePropertyAction.Value>
                            <SolidColorBrush Color="Orange" />
                        </Interactivity1:ChangePropertyAction.Value>
                    </Interactivity1:ChangePropertyAction>
                </Interactivity1:DataTrigger>
            </Interactivity:Interaction.Triggers>
        </Rectangle>

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="48"
                   Foreground="Yellow" FontFamily="Forte" 
                   Text="{Binding Text, Converter={StaticResource toString}, ConverterParameter='The Button Returned: {0}'}" />
        
    </Grid>
</Page>
